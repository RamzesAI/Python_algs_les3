'''
Задание 7. В одномерном массиве целых чисел определить два наименьших элемента.
Они могут быть как равны между собой (оба являться минимальными), так и различаться.
'''
import random
print('')
## генерация списка случайных чисел
rand_ls = []
for i in range(20):
    el = int(random.randint(1, 100))
    rand_ls.append(el)
print(rand_ls)
min_num = rand_ls[0]
list_min_el =[]
for i in rand_ls:
    if i <= min_num:
        min_num = i
        list_min_el.append(min_num)

print(f'Минимальные элементы массива: {list_min_el[-2:]}')